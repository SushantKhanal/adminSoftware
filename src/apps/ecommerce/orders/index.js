import React, { useState } from "react";
import { useSelector } from "react-redux";
import { Layout, Grid } from "antd";

const Orders = () => {
  return <h1>This is Product Orders Page</h1>;
};

export default Orders;
