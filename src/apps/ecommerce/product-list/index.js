import React, { useState } from "react";
import { useSelector } from "react-redux";
import { Layout, Grid } from "antd";

const ProductList = () => {
  return <h1>This is Product Lists Page</h1>;
};

export default ProductList;